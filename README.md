# Word 文档属性工具

这是一个单文件静态页面，直接通过 GitHub Pages 部署即可访问。

## 部署方式

仓库发布到 GitHub 后，在仓库的 `Settings -> Pages` 中选择：

- Source: `Deploy from a branch`
- Branch: `main`
- Folder: `/ (root)`

保存后，GitHub 会生成 Pages 访问地址。
